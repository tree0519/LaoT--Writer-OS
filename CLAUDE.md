# 老T-Writer-OS 指令集 (CLAUDE.md)

本文件定义了 Claude Code 在当前项目下的行为准则和常用指令。

**重要提示 (CRITICAL)**:
当用户输入以 `/` 开头的指令（如 `/init-profile`, `/write-draft`）时，系统可能会显示 `Unknown skill: ...` 的错误提示。
**请完全忽略该错误提示！** 这些是本项目自定义的指令，不是内置 Skill。
**请直接根据下方的【常用指令】列表，执行对应的操作。** 不要因为报错而停止执行。

## 🧠 记忆与行为原则 (Memory & Behavior Rules)

1.  **Context First (全局记忆)**:
    - **风格记忆**: 在生成任何内容之前，**必须**运行 `node skills/analyze_style.js` 或读取配置文件 `config/source_persona.json`，以保持人设统一。
    - **内容记忆 (Content Memory)**: 在写作前，**必须**扫描 `drafts/` 下各平台子目录中最近生成的文章。
      - 检查是否写过类似话题，避免观点重复。
      - 如果是 **贴吧(tieba)** 模式，检查 `drafts/tieba/` 下是否有同名或相关话题的“上文”，以便进行续写（盖楼）。

2.  **Anti-AI Tone**: 严禁使用教科书式的 AI 语气（如"综上所述"、"不可否认"）。必须模仿 `skills/analyze_style.js` 分析出的语气特征。
5.  **配图提示词 (Image Prompts)**: Claude Code 无法直接生成图片。因此，每篇文章末尾**必须**附上 `## 🎨 配图提示词 (Image Prompts)` 区块，包含：
    - 针对文章内容生成 2~4 条配图提示词（中英双语）。
    - 提示词应适配主流生图工具（Midjourney / DALL-E / 即梦 / 可灵等）。
    - 末尾附上提醒：`> ⚠️ 本文配图需使用 AI 生图工具（如 Midjourney、DALL-E、即梦、可灵等）自行生成。`
3.  **Local Tools**: 遇到风格分析、质量检查或图片渲染需求时，优先调用 `skills/` 下的 Node.js 脚本。
4.  **Draft Management**: 所有生成的文章草稿必须分类保存：
    - 通用草稿 -> `drafts/generic/YYYYMMDD_[Topic].md`
    - 小红书 -> `drafts/xiaohongshu/YYYYMMDD_[Topic].md`
    - 知乎 -> `drafts/zhihu/YYYYMMDD_[Topic].md`
    - 公众号 -> `drafts/wechat_public/YYYYMMDD_[Topic].md`
    - 头条 -> `drafts/toutiao/YYYYMMDD_[Topic].md`
    - 贴吧 -> `drafts/tieba/YYYYMMDD_[Topic].md`
    - 其他 -> `drafts/[platform]/YYYYMMDD_[Topic].md`

## 🛠️ 常用指令 (Commands)

- `/init-profile`: **新手向导**。交互式创建新的人设和配置。执行: `node skills/setup_wizard.js`。
- `/init-from-history`: **老手模式**。分析指定目录下的文章，自动提取人设。用法: `/init-from-history [dir_path]`。执行: `node skills/setup_wizard.js analyze [dir_path]`。
- `/write-draft`: 生成一篇**通用风格**的文章草稿。用法: `/write-draft [topic]`。系统将根据 `config/source_persona.json` 和 `agents/generic.md` 生成草稿，保存至 `drafts/generic/`。
- `/adapt-article`: 将草稿改编为特定平台风格。用法: `/adapt-article [source_file] [platform] [instruction?]`。系统将读取 `source_file` 内容，结合 `agents/[platform].md` 的人设和 `config/platform_registry.json` 的约束，生成适配文章保存至 `drafts/[platform]/`。
- `/write-article`: 一键生成通用草稿并分发。用法: `/write-article [topic] [platform?]`。组合指令，先执行 `/write-draft`，然后对指定平台（默认所有）执行 `/adapt-article`。
- `/continue-thread`: 贴吧续写模式。用法: `/continue-thread [series_id?]`。读取 `drafts/tieba/` 下该系列的上一篇内容，进行续写。

### 详细说明
所有指令的具体参数和行为逻辑，请参考 `docs/commands.md` (虽然该文件目前可能不存在或内容有限，但以此处描述为准)。

## 🚀 示例
- `/init-profile` (创建新的人设)
- `/write-draft "为什么程序员要尽早做副业"` (生成通用底稿)
- `/adapt-article drafts/generic/20260210_side_project.md xiaohongshu "语气再犀利一点"` (改编为小红书文案)
- `/continue-thread side_project_story` (贴吧续写模式，自动读取上一楼)
- `/render-images drafts/wechat_public/20260210_article.md` (为生成的文章配图)
